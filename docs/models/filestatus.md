# FileStatus

## Example Usage

```python
from flypix.models import FileStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: FileStatus = "INITIATED"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"INITIATED"`
- `"UPLOADED"`
- `"REGISTERED"`
- `"READY"`
