# ResponseStatus

## Example Usage

```python
from flypix.models import ResponseStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: ResponseStatus = "SUCCESS"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"SUCCESS"`
- `"FAILED"`
- `"DELETED"`
- `"APPLIED"`
