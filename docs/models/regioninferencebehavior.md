# RegionInferenceBehavior

## Example Usage

```python
from flypix.models import RegionInferenceBehavior

# Open enum: unrecognized values are captured as UnrecognizedStr
value: RegionInferenceBehavior = "USE"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"USE"`
- `"IGNORE"`
