components:
  schemas:
    AnnotationPayloadInput:
      additionalProperties: false
      properties:
        confidence:
          format: "double"
          maximum: 1
          minimum: 0
          type: "number"
        polygon:
          type: "string"
      required:
        - "polygon"
        - "confidence"
      type: "object"
    ApplyChangeDetectionInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ApplyChangeDetectionInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        raster_id_a:
          type: "string"
        raster_id_b:
          type: "string"
      required:
        - "raster_id_a"
        - "raster_id_b"
      type: "object"
    ApplyModelInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ApplyModelInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        raster_id:
          type: "string"
      required:
        - "raster_id"
      type: "object"
    ApplyModelResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ApplyModelResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        data:
          additionalProperties: {}
          type: "object"
        inference_id:
          type: "string"
        status:
          type: "string"
      required:
        - "status"
        - "inference_id"
      type: "object"
    ArtifactResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ArtifactResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        artifact_data:
          additionalProperties: {}
          type: "object"
        artifact_id:
          type: "string"
        created_at:
          format: "date-time"
          type: "string"
        project_id:
          type: "string"
        session_id:
          type: "string"
        tenant_id:
          type: "string"
        type:
          type: "string"
      required:
        - "artifact_id"
        - "session_id"
        - "tenant_id"
        - "project_id"
        - "created_at"
        - "type"
        - "artifact_data"
      type: "object"
    Balance:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/Balance.json"
          format: "uri"
          readOnly: true
          type: "string"
        credits:
          format: "double"
          type: "number"
        withheld:
          format: "double"
          type: "number"
      required:
        - "credits"
        - "withheld"
      type: "object"
    Bounds3857:
      additionalProperties: false
      properties:
        maxx:
          format: "double"
          type: "number"
        maxy:
          format: "double"
          type: "number"
        minx:
          format: "double"
          type: "number"
        miny:
          format: "double"
          type: "number"
      required:
        - "minx"
        - "miny"
        - "maxx"
        - "maxy"
      type: "object"
    COGRasterSource:
      additionalProperties: false
      properties:
        type:
          type: "string"
        url:
          type: "string"
      required:
        - "type"
        - "url"
      type: "object"
    ChatMessageResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ChatMessageResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        context:
          items:
            $ref: "#/components/schemas/MessageContext"
          type:
            - "array"
            - "null"
        created_at:
          format: "date-time"
          type: "string"
        response:
          type: "string"
        session_id:
          type: "string"
      required:
        - "session_id"
        - "response"
        - "created_at"
      type: "object"
    ClassResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ClassResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        class_id:
          type: "string"
        color:
          type: "string"
        name:
          type: "string"
      required:
        - "class_id"
        - "name"
        - "color"
      type: "object"
    CreateAnnotationsInVectorInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateAnnotationsInVectorInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        class_id:
          type: "string"
        payload:
          items:
            $ref: "#/components/schemas/AnnotationPayloadInput"
          type:
            - "array"
            - "null"
      required:
        - "class_id"
        - "payload"
      type: "object"
    CreateClassInProjectInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateClassInProjectInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        color:
          pattern: "^[a-fA-F0-9]{6}$"
          type: "string"
        name:
          maxLength: 64
          minLength: 1
          type: "string"
      required:
        - "name"
      type: "object"
    CreateEmptyVectorInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateEmptyVectorInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        name:
          maxLength: 50
          minLength: 1
          type: "string"
        raster_id:
          type: "string"
      required:
        - "raster_id"
      type: "object"
    CreateFolderInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateFolderInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        name:
          maxLength: 200
          minLength: 3
          type: "string"
        parent_id:
          type: "string"
        project_id:
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "tenant_id"
        - "project_id"
        - "name"
      type: "object"
    CreateProjectInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateProjectInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        name:
          maxLength: 50
          minLength: 3
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "tenant_id"
        - "name"
      type: "object"
    CreateRegionsInRasterInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateRegionsInRasterInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        region_payloads:
          items:
            $ref: "#/components/schemas/RegionPayloadInput"
          type:
            - "array"
            - "null"
      required:
        - "region_payloads"
      type: "object"
    CreateSessionArtifactInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/CreateSessionArtifactInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        artifact_data:
          additionalProperties: {}
          type: "object"
        type:
          enum:
            - "data"
          type: "string"
      required:
        - "artifact_data"
        - "type"
      type: "object"
    EntityDeletionResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/EntityDeletionResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        id:
          type: "string"
        status:
          type: "string"
      required:
        - "id"
        - "status"
      type: "object"
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: "Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'"
          type: "string"
        message:
          description: "Error message text"
          type: "string"
        value:
          description: "The value at the given location"
      type: "object"
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ErrorModel.json"
          format: "uri"
          readOnly: true
          type: "string"
        detail:
          description: "A human-readable explanation specific to this occurrence of the problem."
          examples:
            - "Property foo is required but is missing."
          type: "string"
        errors:
          description: "Optional list of individual error details"
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - "array"
            - "null"
        instance:
          description: "A URI reference that identifies the specific occurrence of the problem."
          examples:
            - "https://example.com/error-log/abc123"
          format: "uri"
          type: "string"
        status:
          description: "HTTP status code"
          examples:
            - 400
          format: "int64"
          type: "integer"
        title:
          description: "A short, human-readable summary of the problem type. This value should not change between occurrences of the error."
          examples:
            - "Bad Request"
          type: "string"
        type:
          default: "about:blank"
          description: "A URI reference to human-readable documentation for the error."
          examples:
            - "https://example.com/errors/example"
          format: "uri"
          type: "string"
      type: "object"
    ErroredAnnotation:
      additionalProperties: false
      properties:
        Index:
          format: "int64"
          type: "integer"
        Message:
          type: "string"
      required:
        - "Index"
        - "Message"
      type: "object"
    EstimateModelApplicationInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/EstimateModelApplicationInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        raster_id:
          type: "string"
      required:
        - "raster_id"
      type: "object"
    EstimateModelResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/EstimateModelResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        total_cost:
          format: "int64"
          type: "integer"
        total_pixels:
          format: "int64"
          type: "integer"
      required:
        - "total_cost"
        - "total_pixels"
      type: "object"
    FileLinkResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/FileLinkResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        url:
          type: "string"
      required:
        - "url"
      type: "object"
    FolderResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/FolderResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        folder_id:
          type: "string"
        name:
          type: "string"
        parent_id:
          type:
            - "string"
            - "null"
      required:
        - "folder_id"
        - "name"
        - "parent_id"
      type: "object"
    GetStorageUsageOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/GetStorageUsageOutputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        bytes:
          format: "int64"
          type: "integer"
        storage_quota:
          format: "int64"
          type: "integer"
        tenant_id:
          type: "string"
      required:
        - "tenant_id"
        - "bytes"
        - "storage_quota"
      type: "object"
    HealthOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/HealthOutputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        status:
          type: "string"
      required:
        - "status"
      type: "object"
    InferenceResponse:
      additionalProperties: false
      properties:
        created_at:
          format: "date-time"
          type: "string"
        inference_id:
          type: "string"
        model_id:
          type:
            - "string"
            - "null"
        project_id:
          type: "string"
        status:
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "inference_id"
        - "tenant_id"
        - "project_id"
        - "model_id"
        - "status"
        - "created_at"
      type: "object"
    LoginResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/LoginResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        access_token:
          type: "string"
        expires_in:
          format: "int64"
          type: "integer"
        refresh_token:
          type: "string"
        token_type:
          type: "string"
      required:
        - "access_token"
        - "token_type"
        - "expires_in"
      type: "object"
    LoginWithPasswordInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/LoginWithPasswordInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        password:
          type: "string"
        username:
          type: "string"
      required:
        - "username"
        - "password"
      type: "object"
    MessageContext:
      additionalProperties: false
      properties:
        artifact_id:
          type: "string"
        type:
          type: "string"
      required:
        - "artifact_id"
        - "type"
      type: "object"
    MessageContextInput:
      additionalProperties: false
      properties:
        artifact_id:
          type: "string"
        type:
          enum:
            - "data"
          type: "string"
      required:
        - "artifact_id"
        - "type"
      type: "object"
    OSMRasterSource:
      additionalProperties: false
      properties:
        extent:
          $ref: "#/components/schemas/Bounds3857"
        type:
          type: "string"
      required:
        - "type"
        - "extent"
      type: "object"
    OfficialModelResponse:
      additionalProperties: false
      properties:
        created_at:
          format: "date-time"
          type: "string"
        metadata:
          additionalProperties: {}
          type: "object"
        model_id:
          type: "string"
        name:
          type: "string"
        status:
          type: "string"
      required:
        - "name"
        - "status"
        - "model_id"
        - "metadata"
        - "created_at"
      type: "object"
    PrivateModelResponse:
      additionalProperties: false
      properties:
        created_at:
          format: "date-time"
          type: "string"
        metadata:
          additionalProperties: {}
          type: "object"
        model_id:
          type: "string"
        name:
          type: "string"
        project_id:
          type: "string"
        status:
          type: "string"
        tenant_id:
          type: "string"
        training_id:
          type:
            - "string"
            - "null"
      required:
        - "tenant_id"
        - "project_id"
        - "training_id"
        - "name"
        - "status"
        - "model_id"
        - "metadata"
        - "created_at"
      type: "object"
    ProjectResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/ProjectResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        created_at:
          format: "date-time"
          type: "string"
        name:
          type: "string"
        project_id:
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "tenant_id"
        - "project_id"
        - "name"
        - "created_at"
      type: "object"
    RasterResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/RasterResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        created_at:
          format: "date-time"
          type: "string"
        folder_id:
          type:
            - "string"
            - "null"
        name:
          type: "string"
        project_id:
          type:
            - "string"
            - "null"
        raster_id:
          type: "string"
        source:
          discriminator:
            mapping:
              EXTERNAL_COG: "#/components/schemas/COGRasterSource"
              OSM: "#/components/schemas/OSMRasterSource"
              UPLOAD: "#/components/schemas/UploadRasterSource"
              WMTS: "#/components/schemas/WMTSRasterSource"
            propertyName: "type"
          oneOf:
            - $ref: "#/components/schemas/UploadRasterSource"
            - $ref: "#/components/schemas/WMTSRasterSource"
            - $ref: "#/components/schemas/COGRasterSource"
            - $ref: "#/components/schemas/OSMRasterSource"
        status:
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "raster_id"
        - "tenant_id"
        - "project_id"
        - "folder_id"
        - "name"
        - "status"
        - "created_at"
        - "source"
      type: "object"
    RasterTreeNode:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/RasterTreeNode.json"
          format: "uri"
          readOnly: true
          type: "string"
        id:
          type: "string"
        nodes:
          items:
            $ref: "#/components/schemas/RasterTreeNode"
          type:
            - "array"
            - "null"
        rasters:
          items:
            type: "string"
          type:
            - "array"
            - "null"
      required:
        - "id"
        - "rasters"
        - "nodes"
      type: "object"
    RefreshTokenInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/RefreshTokenInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        refresh_token:
          type: "string"
      required:
        - "refresh_token"
      type: "object"
    RegionMetadata:
      additionalProperties: false
      properties:
        color:
          type: "string"
        description:
          type:
            - "string"
            - "null"
        name:
          type:
            - "string"
            - "null"
      required:
        - "color"
        - "name"
        - "description"
      type: "object"
    RegionMetadataInput:
      additionalProperties: false
      properties:
        color:
          pattern: "^[a-fA-F0-9]{6}$"
          type: "string"
        description:
          maxLength: 150
          type: "string"
        name:
          maxLength: 50
          type: "string"
      required:
        - "color"
      type: "object"
    RegionPayloadInput:
      additionalProperties: false
      properties:
        inference_behavior:
          enum:
            - "USE"
            - "IGNORE"
          type: "string"
        metadata:
          $ref: "#/components/schemas/RegionMetadataInput"
        polygon:
          type: "string"
        training_behavior:
          enum:
            - "TRAINING"
            - "VALIDATION"
            - "TEST"
            - "IGNORE"
          type: "string"
      required:
        - "polygon"
        - "metadata"
        - "training_behavior"
        - "inference_behavior"
      type: "object"
    RegionResponse:
      additionalProperties: false
      properties:
        inference_behavior:
          type: "string"
        metadata:
          $ref: "#/components/schemas/RegionMetadata"
        polygon:
          type: "string"
        project_id:
          type: "string"
        raster_id:
          type: "string"
        region_id:
          type: "string"
        tenant_id:
          type: "string"
        training_behavior:
          type: "string"
      required:
        - "region_id"
        - "tenant_id"
        - "project_id"
        - "raster_id"
        - "polygon"
        - "metadata"
        - "training_behavior"
        - "inference_behavior"
      type: "object"
    SendMessageInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/SendMessageInputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        context:
          items:
            $ref: "#/components/schemas/MessageContextInput"
          type:
            - "array"
            - "null"
        user_message:
          maxLength: 10000
          minLength: 1
          type: "string"
        vector_ids:
          items:
            type: "string"
          type:
            - "array"
            - "null"
      required:
        - "user_message"
        - "vector_ids"
      type: "object"
    TenantResponse:
      additionalProperties: false
      properties:
        name:
          type: "string"
        tenant_id:
          type: "string"
      required:
        - "tenant_id"
        - "name"
      type: "object"
    Transaction:
      additionalProperties: false
      properties:
        created_at:
          format: "date-time"
          type: "string"
        credits:
          format: "double"
          type: "number"
        project_id:
          type:
            - "string"
            - "null"
        reason:
          type: "string"
        reference_id:
          type:
            - "string"
            - "null"
        tenant_id:
          type: "string"
        transaction_id:
          type: "string"
        type:
          type: "string"
      required:
        - "transaction_id"
        - "tenant_id"
        - "project_id"
        - "reference_id"
        - "credits"
        - "reason"
        - "type"
        - "created_at"
      type: "object"
    UploadAnnotationsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/UploadAnnotationsOutputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        annotation_ids:
          items:
            format: "int64"
            type: "integer"
          type:
            - "array"
            - "null"
        errored:
          items:
            $ref: "#/components/schemas/ErroredAnnotation"
          type:
            - "array"
            - "null"
      required:
        - "annotation_ids"
        - "errored"
      type: "object"
    UploadOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/UploadOutputBody.json"
          format: "uri"
          readOnly: true
          type: "string"
        raster_id:
          type: "string"
        status:
          type: "string"
      required:
        - "raster_id"
        - "status"
      type: "object"
    UploadRasterSource:
      additionalProperties: false
      properties:
        processed_file_id:
          type:
            - "string"
            - "null"
        reprojected_file_id:
          type:
            - "string"
            - "null"
        type:
          type: "string"
        uploaded_file_id:
          type: "string"
      required:
        - "type"
        - "uploaded_file_id"
        - "processed_file_id"
        - "reprojected_file_id"
      type: "object"
    VectorFeatures:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/VectorFeatures.json"
          format: "uri"
          readOnly: true
          type: "string"
        classes:
          additionalProperties:
            type: "string"
          type: "object"
        data:
          $ref: "#/components/schemas/VectorFeaturesData"
      required:
        - "data"
        - "classes"
      type: "object"
    VectorFeaturesData:
      additionalProperties: false
      properties:
        areas:
          items:
            format: "double"
            type: "number"
          type:
            - "array"
            - "null"
        class_ids:
          items:
            format: "int64"
            type: "integer"
          type:
            - "array"
            - "null"
        confidences:
          items:
            format: "double"
            type: "number"
          type:
            - "array"
            - "null"
        ids:
          items:
            format: "int64"
            type: "integer"
          type:
            - "array"
            - "null"
        polygons:
          items:
            type: "string"
          type:
            - "array"
            - "null"
      required:
        - "ids"
        - "areas"
        - "polygons"
        - "confidences"
        - "class_ids"
      type: "object"
    VectorMetadata:
      additionalProperties: false
      properties:
        source:
          type: "string"
      required:
        - "source"
      type: "object"
    VectorResponse:
      additionalProperties: false
      properties:
        $schema:
          description: "A URL to the JSON Schema for this object."
          examples:
            - "https://example.com/schemas/VectorResponse.json"
          format: "uri"
          readOnly: true
          type: "string"
        metadata:
          $ref: "#/components/schemas/VectorMetadata"
        name:
          type: "string"
        project_id:
          type: "string"
        tenant_id:
          type: "string"
        vector_id:
          type: "string"
      required:
        - "tenant_id"
        - "project_id"
        - "vector_id"
        - "metadata"
        - "name"
      type: "object"
    WMTSRasterSource:
      additionalProperties: false
      properties:
        layer:
          type: "string"
        type:
          type: "string"
        url:
          type: "string"
      required:
        - "type"
        - "layer"
        - "url"
      type: "object"
  securitySchemes:
    ApiKey:
      description: "Paste your API key here to use it with every request."
      in: "header"
      name: "ApiKey"
      type: "apiKey"
    BearerAuth:
      bearerFormat: "JWT"
      description: "Paste your Bearer token here to use it with every request."
      scheme: "bearer"
      type: "http"
info:
  description: "\nPublic OpenAPI Schema\n\nThis API lets you interact directly with the application with HTTP requests.\n\nYou will need to set a valid authorization header for every request:\n\n`Authorization: Bearer <token>`\n\nExample use case: Find an existing raster and its vector features:\n\n```\nPOST /auth/login/password -> Fetch token and use it in subsequent requests\nGET /users/tenant/for-user/ -> Get your tenant id for subsequent requests\nGET /projects/for-tenant/{tenant_id} -> Find your project\nGET /rasters/for-project/{project_id}/tree -> Find your raster\nGET /vectors/for-raster/{raster_id} -> Get the vector ids for the raster\nGET /vectors/{vector_id}/features -> Get the annotations\n```\n"
  title: "Public API"
  version: "2.0.1"
openapi: "3.1.0"
paths:
  /health:
    get:
      description: "Reports whether the API is up. Does not check downstream backends."
      operationId: "health"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      security: []
      summary: "Health check"
      tags:
        - "system"
  /v2/auth/login/password:
    post:
      description: "Exchanges a username and password for a token. Not available when the tenant's identity provider is DESP."
      operationId: "login-with-password"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginWithPasswordInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      security: []
      summary: "Login with password"
      tags:
        - "auth"
  /v2/auth/refresh:
    post:
      description: "Exchanges a refresh token for a new token. Not available when the tenant's identity provider is DESP."
      operationId: "refresh-token"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshTokenInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      security: []
      summary: "Refresh token"
      tags:
        - "auth"
  /v2/files/upload:
    post:
      description: "Uploads a file directly, streamed from the request body. Rejected if it would push the tenant over its subscription's storage quota."
      operationId: "files_upload_v2"
      parameters:
        - explode: false
          in: "query"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "folder_id"
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "filename"
          required: true
          schema:
            type: "string"
        - in: "header"
          name: "Content-Type"
          schema:
            type: "string"
        - in: "header"
          name: "Content-Length"
          schema:
            type: "string"
      requestBody:
        content:
          application/octet-stream:
            schema:
              contentMediaType: "application/octet-stream"
              format: "binary"
              type: "string"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Upload a file"
      tags:
        - "files"
      x-speakeasy-name-override: upload_v2
  /v2/files/upload-from-url:
    post:
      description: "Uploads a file by fetching it from a remote URL; the download is streamed straight into the upload, never buffered."
      operationId: "files_upload_from_url"
      parameters:
        - explode: false
          in: "query"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "folder_id"
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "filename"
          required: true
          schema:
            type: "string"
        - in: "header"
          name: "Content-Type"
          schema:
            type: "string"
        - in: "header"
          name: "Content-Length"
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "file_url"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Upload a file from a URL"
      tags:
        - "files"
      x-speakeasy-name-override: upload_from_url
  /v2/files/usage/{tenant_id}:
    get:
      description: "Returns a tenant's storage usage against its subscription quota."
      operationId: "get-storage-usage"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetStorageUsageOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get storage usage"
      tags:
        - "files"
  /v2/files/{file_id}/download:
    get:
      description: "Returns a time-limited download link for a file."
      operationId: "get-file-download-link"
      parameters:
        - in: "path"
          name: "file_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileLinkResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get file download link"
      tags:
        - "files"
  /v2/folders/:
    post:
      description: "Creates a folder, in the project root if `parent_id` is omitted or inside another folder otherwise."
      operationId: "create-folder"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFolderInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FolderResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create folder"
      tags:
        - "folders"
  /v2/folders/for-folder/{folder_id}:
    get:
      description: "Lists the folders directly inside a folder. Not recursive."
      operationId: "get-folders-for-folder"
      parameters:
        - in: "path"
          name: "folder_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/FolderResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List subfolders"
      tags:
        - "folders"
  /v2/folders/for-project/{project_id}:
    get:
      description: "Lists the folders in a project's root. Not recursive."
      operationId: "get-folders-in-project-root"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/FolderResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List root folders"
      tags:
        - "folders"
  /v2/folders/get-by-ids:
    post:
      description: "Returns the folders matching the given ids, in one call."
      operationId: "get-folders-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/FolderResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get folders by ids"
      tags:
        - "folders"
  /v2/folders/{folder_id}:
    delete:
      description: "Deletes a folder and everything under it, recursively. This cannot be undone."
      operationId: "delete-folder"
      parameters:
        - in: "path"
          name: "folder_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityDeletionResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete folder"
      tags:
        - "folders"
  /v2/geosense/session/{project_id}/:
    post:
      description: "Starts a new geosense chat session for a project."
      operationId: "create-session-in-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: "string"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Start geosense session"
      tags:
        - "geosense"
  /v2/geosense/{session_id}/artifact/:
    post:
      description: "Creates an artifact in a geosense session."
      operationId: "create-session-artifact"
      parameters:
        - in: "path"
          name: "session_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSessionArtifactInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create session artifact"
      tags:
        - "geosense"
  /v2/geosense/{session_id}/artifact/{artifact_id}:
    get:
      description: "Returns an artifact from a geosense session."
      operationId: "get-session-artifact"
      parameters:
        - in: "path"
          name: "session_id"
          required: true
          schema:
            type: "string"
        - in: "path"
          name: "artifact_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get session artifact"
      tags:
        - "geosense"
  /v2/geosense/{session_id}/message/:
    post:
      description: "Sends a chat message in a geosense session, optionally grounded in prior artifacts via `context`."
      operationId: "send-message"
      parameters:
        - in: "path"
          name: "session_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatMessageResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Send geosense message"
      tags:
        - "geosense"
  /v2/inferences/for-project/{project_id}:
    get:
      description: "Lists the inferences in a project."
      operationId: "get-inferences-for-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/InferenceResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List project inferences"
      tags:
        - "inferences"
  /v2/inferences/for-raster/{raster_id}:
    get:
      description: "Lists the inferences for a raster."
      operationId: "get-inferences-for-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/InferenceResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List raster inferences"
      tags:
        - "inferences"
  /v2/inferences/for-tenant/{tenant_id}:
    get:
      description: "Lists the inferences in a tenant."
      operationId: "get-inferences-for-tenant"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/InferenceResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List tenant inferences"
      tags:
        - "inferences"
  /v2/inferences/get-by-ids:
    post:
      description: "Returns the inferences matching the given ids, in one call."
      operationId: "get-inferences-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/InferenceResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get inferences by ids"
      tags:
        - "inferences"
  /v2/models/change-detection/apply:
    post:
      description: "Triggers asynchronous change-detection inference between two rasters."
      operationId: "apply-change-detection"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplyChangeDetectionInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyModelResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Apply change detection"
      tags:
        - "models"
  /v2/models/for-project/{project_id}:
    get:
      description: "Lists the models available to be applied on any file in a project."
      operationId: "get-models-for-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/PrivateModelResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List project models"
      tags:
        - "models"
  /v2/models/for-tenant/{tenant_id}:
    get:
      description: "Lists the models available in a tenant."
      operationId: "get-models-for-tenant"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/PrivateModelResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List tenant models"
      tags:
        - "models"
  /v2/models/get-by-ids:
    post:
      description: "Returns the models matching the given ids, in one call."
      operationId: "get-models-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/PrivateModelResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get models by ids"
      tags:
        - "models"
  /v2/models/inference/{inference_id}:
    get:
      description: "Returns the current status of an inference from applying a model."
      operationId: "get-inference-status"
      parameters:
        - in: "path"
          name: "inference_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyModelResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get inference status"
      tags:
        - "models"
  /v2/models/official:
    get:
      description: "Lists official models, available to be applied on any file in any project."
      operationId: "get-official-models"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/OfficialModelResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List official models"
      tags:
        - "models"
  /v2/models/{model_id}:
    delete:
      description: "Deletes an already-trained model. This cannot be undone."
      operationId: "delete-model"
      parameters:
        - in: "path"
          name: "model_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityDeletionResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete model"
      tags:
        - "models"
  /v2/models/{model_id}/apply:
    post:
      description: "Triggers asynchronous inference of a model on a raster. Returns 402 if the estimated cost exceeds the tenant's balance, unless its subscription grants unlimited credits."
      operationId: "apply-model-on-raster"
      parameters:
        - in: "path"
          name: "model_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplyModelInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyModelResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Apply model to raster"
      tags:
        - "models"
  /v2/models/{model_id}/apply/estimate:
    post:
      description: "Calculates the credit cost to apply a model on a raster, without triggering it."
      operationId: "estimate-model-application-on-file"
      parameters:
        - in: "path"
          name: "model_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EstimateModelApplicationInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EstimateModelResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Estimate model application cost"
      tags:
        - "models"
  /v2/payments/balance/{tenant_id}:
    get:
      description: "Returns a tenant's current credit balance."
      operationId: "get-tenant-balance"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Balance"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get tenant balance"
      tags:
        - "payments"
  /v2/payments/balance/{tenant_id}/transactions:
    get:
      description: "Lists a tenant's credit ledger entries, paginated."
      operationId: "get-tenant-transactions"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Transaction"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List tenant transactions"
      tags:
        - "payments"
  /v2/projects/:
    post:
      description: "Creates a new empty project."
      operationId: "create-project"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create project"
      tags:
        - "projects"
  /v2/projects/for-tenant/{tenant_id}:
    get:
      description: "Lists a tenant's projects."
      operationId: "get-projects-for-tenant"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/ProjectResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List projects for tenant"
      tags:
        - "projects"
  /v2/projects/{project_id}:
    delete:
      description: "Deletes a project and everything under it, recursively. This cannot be undone."
      operationId: "delete-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityDeletionResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete project"
      tags:
        - "projects"
  /v2/rasters/for-project/{project_id}:
    get:
      description: "Lists the rasters in a project, optionally scoped to a single folder via `folder_id` (\"root\", a folder UUID, or omitted for all)."
      operationId: "get-rasters-for-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
        - description: "A specific folder UUID or \"root\". Omit to list all rasters in the project regardless of folder."
          explode: false
          in: "query"
          name: "folder_id"
          schema:
            description: "A specific folder UUID or \"root\". Omit to list all rasters in the project regardless of folder."
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RasterResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List project rasters"
      tags:
        - "rasters"
  /v2/rasters/for-project/{project_id}/tree:
    get:
      description: "Returns a project's full folder structure with the raster ids in each folder, in one call — used to build breadcrumbs/navigation without a separate listing call."
      operationId: "get-project-raster-tree"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RasterTreeNode"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get project raster tree"
      tags:
        - "rasters"
  /v2/rasters/for-tenant/{tenant_id}:
    get:
      description: "Lists the rasters in a tenant."
      operationId: "get-rasters-for-tenant"
      parameters:
        - in: "path"
          name: "tenant_id"
          required: true
          schema:
            type: "string"
        - description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
          explode: false
          in: "query"
          name: "filter"
          schema:
            description: "Comma-separated field:operator:value clauses, e.g. status:eq:FAILED"
            type: "string"
        - description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
          explode: false
          in: "query"
          name: "sort"
          schema:
            description: "Comma-separated field:asc|desc clauses, e.g. created_at:desc"
            type: "string"
        - explode: false
          in: "query"
          name: "offset"
          schema:
            default: 0
            format: "int64"
            type: "integer"
        - explode: false
          in: "query"
          name: "limit"
          schema:
            default: 200
            format: "int64"
            type: "integer"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RasterResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List tenant rasters"
      tags:
        - "rasters"
  /v2/rasters/get-by-ids:
    post:
      description: "Returns the rasters matching the given ids, in one call."
      operationId: "get-rasters-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RasterResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get rasters by ids"
      tags:
        - "rasters"
  /v2/rasters/{raster_id}:
    delete:
      description: "Deletes a raster, cascading to its backing file if it has one. This cannot be undone."
      operationId: "delete-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityDeletionResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete raster"
      tags:
        - "rasters"
    get:
      description: "Returns a raster's details, by id."
      operationId: "get-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RasterResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get raster details"
      tags:
        - "rasters"
  /v2/regions/:
    delete:
      description: "Deletes many regions at once."
      operationId: "delete-regions"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/EntityDeletionResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete regions"
      tags:
        - "regions"
  /v2/regions/for-raster/{raster_id}:
    get:
      description: "Lists the regions for a raster."
      operationId: "get-regions-for-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RegionResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List raster regions"
      tags:
        - "regions"
  /v2/regions/get-by-ids:
    post:
      description: "Returns the regions matching the given ids, in one call."
      operationId: "get-regions-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RegionResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get regions by ids"
      tags:
        - "regions"
  /v2/regions/{raster_id}:
    post:
      description: "Creates regions on a raster. Region payloads use the same shape as vector features."
      operationId: "create-regions-in-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRegionsInRasterInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RegionResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create regions on a raster"
      tags:
        - "regions"
  /v2/users/tenants/for-user/:
    get:
      description: "Lists the tenants the authenticated user has access to."
      operationId: "get-tenants-for-user"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/TenantResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List tenants for user"
      tags:
        - "users"
  /v2/vectors/:
    post:
      description: "Creates a new empty vector layer on a raster, ready to receive annotations."
      operationId: "create-empty-vector"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmptyVectorInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VectorResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create empty vector layer"
      tags:
        - "vectors"
  /v2/vectors/classes/for-project/{project_id}:
    get:
      description: "Lists the classes for a project."
      operationId: "get-classes-for-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/ClassResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List project classes"
      tags:
        - "vectors"
  /v2/vectors/for-raster/{raster_id}:
    get:
      description: "Lists the vector layers for a raster. This won't return the vector features, just the names and identifiers of the vector layers."
      operationId: "get-vectors-for-raster"
      parameters:
        - in: "path"
          name: "raster_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/VectorResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List raster vector layers"
      tags:
        - "vectors"
  /v2/vectors/get-by-ids:
    post:
      description: "Returns the vector layers matching the given ids, in one call."
      operationId: "get-vectors-by-ids"
      requestBody:
        content:
          application/json:
            schema:
              items:
                type: "string"
              type:
                - "array"
                - "null"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/VectorResponse"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Get vector layers by ids"
      tags:
        - "vectors"
  /v2/vectors/{project_id}/class:
    post:
      description: "Creates a class in a project. Classes are shared among all vectors in a project."
      operationId: "create-class-in-project"
      parameters:
        - in: "path"
          name: "project_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClassInProjectInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClassResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create project class"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/:
    delete:
      description: "Deletes a vector and its annotations. This cannot be undone."
      operationId: "delete-vector"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityDeletionResponse"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Delete vector"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/annotations:
    post:
      description: "Creates annotations in a vector from raw geometry payloads. Invalid geometry is rejected with a generic 400, not per-item validation errors."
      operationId: "create-annotations-in-vector"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAnnotationsInVectorInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  format: "int64"
                  type: "integer"
                type:
                  - "array"
                  - "null"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Create annotations in a vector"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/features:
    get:
      description: "Returns the vector features (annotations) of a vector."
      operationId: "get-vector-features"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VectorFeatures"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "List vector annotations"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/upload-geojson/:
    post:
      description: "Creates annotations by uploading a GeoJSON file. Only Polygon features in EPSG:3857 are accepted; per-feature failures are collected into the response's `errored` list instead of failing the whole upload."
      operationId: "upload-geojson"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          multipart/form-data:
            encoding:
              file:
                contentType: "application/octet-stream"
            schema:
              properties:
                file:
                  contentEncoding: "binary"
                  contentMediaType: "application/octet-stream"
                  format: "binary"
                  type: "string"
              required:
                - "file"
              type: "object"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadAnnotationsOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Upload GeoJSON annotations"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/upload-gpkg/:
    post:
      description: "Creates annotations by uploading a GeoPackage file. Same partial-failure behavior as the GeoJSON upload."
      operationId: "upload_geopkg"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          multipart/form-data:
            encoding:
              file:
                contentType: "application/octet-stream"
            schema:
              properties:
                file:
                  contentEncoding: "binary"
                  contentMediaType: "application/octet-stream"
                  format: "binary"
                  type: "string"
              required:
                - "file"
              type: "object"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadAnnotationsOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Upload GeoPackage annotations"
      tags:
        - "vectors"
  /v2/vectors/{vector_id}/upload-shp/:
    post:
      description: "Creates annotations by uploading a zipped Shapefile. Same partial-failure behavior as the GeoJSON upload."
      operationId: "upload_shapefile"
      parameters:
        - in: "path"
          name: "vector_id"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          multipart/form-data:
            encoding:
              file:
                contentType: "application/octet-stream"
            schema:
              properties:
                file:
                  contentEncoding: "binary"
                  contentMediaType: "application/octet-stream"
                  format: "binary"
                  type: "string"
              required:
                - "file"
              type: "object"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadAnnotationsOutputBody"
          description: "OK"
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: "Error"
      summary: "Upload Shapefile annotations"
      tags:
        - "vectors"
servers:
  - url: https://api.flypix.ai
    description: FlyPix production API
tags:
  - name: Auth
    description: Obtain and refresh API access tokens.
  - name: Files
    description: Upload, download, list, and delete files.
  - name: Folders
    description: Organize files into folders within a project.
  - name: Geosense
    description: GeoSense sessions, artifacts, and messages.
  - name: Inferences
    description: Inference results across tenants, projects, and files.
  - name: Models
    description: List, apply, and manage detection models.
  - name: Payments
    description: Billing and tenant balance.
  - name: Projects
    description: Manage projects within a tenant.
  - name: Rasters
    description: Raster imagery associated with files and projects.
  - name: Regions
    description: Regions of interest within a file.
  - name: Users
    description: User account details and tenant membership.
  - name: Vectors
    description: Vector data — classes, features, and annotations.
security:
  - ApiKey: []
  - BearerAuth: []
